import { IpaddressformatDirective } from './ipaddressformat.directive';

describe('IpaddressformatDirective', () => {
  it('should create an instance', () => {
    const directive = new IpaddressformatDirective();
    expect(directive).toBeTruthy();
  });
});
